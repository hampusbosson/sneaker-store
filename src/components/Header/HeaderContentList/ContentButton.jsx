function ContentButton({name}) {
    return (
        <div>
            <button className="text-black text-lg">
                {name}
            </button>
        </div>
    );
}

export default ContentButton